# Random Walker CLI Implementation (badly) written in rust.

## building  
build with `cargo build --release` and run with `cargo run`.  
the executable will be in /target/release/ with the name "random_walker".

